# Changelog

## v1.1.0

### 功能
- 引入 i18n，仅支持中文与英文；语言包按需懒加载（构建产物中的 `zh-CN` / `en-US` 独立分包）。语言跟随后端 `/api/config` 的 `default_language` 与浏览器本地语言。
- 顶部工具栏新增语言切换按钮（中/EN）。
- 明/暗主题切换使用 View Transitions API 实现从点击处圆形展开的动画（Element Plus 风格）；主题仅亮色/暗色两档，默认亮色，不跟随系统。
- 首页卡片第三列：有到期日即始终显示「剩余 X 天」，剩余价值免费时显示「免费」，未配价格时不展示价值行；价格徽章免费显示「免费」、有价格显示如「$10.99/年」。剩余天数/剩余价值遵守后端 `show_expire` / `show_price` 开关。
- 流量配额未设置或无限时统一显示 `∞` 符号。
- 首页卡片延迟/丢包迷你柱改为 7px 纤细方块；悬浮提示改为「上：时间，下：四线具体值（含 BGP）」两行结构。
- 详情页存储信息内三个卡片（内存/交换/硬盘）与网络信息内两个卡片占满各自卡片剩余空间；存储信息「硬盘」在移动端独占一行。

### 修复
- 修复未配置价格/到期信息的服务器，首页卡片最后一列退化为显示负载（读到全 0）的问题。
- 修复详情页顶部切换机器的下拉选项弹窗被页面内高层级容器遮挡的问题：select 选项弹层改为挂载到 `body` 顶层（`SelectPortal` + `fixed` 定位），并在模态弹窗内保持可交互。

## v1.0.0

玻璃拟态主题首个正式版。